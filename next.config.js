/** @type {import('next').NextConfig} */
const nextConfig = {i18n: {
    locales: ['mn', 'jp'], // List all the languages you want to support
    defaultLocale: 'mn', // Default language
  },}

module.exports = nextConfig
